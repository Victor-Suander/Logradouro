package com.victors.sistemalogradouro;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

// Indica que essa é a classe principal do Spring Boot
@SpringBootApplication
public class SistemaLogradouroApplication {

    // Método inicial da aplicação
    public static void main(String[] args) {

        // Inicializa o Spring Boot
        SpringApplication.run(SistemaLogradouroApplication.class, args);
    }
}